# Vite picks this up automatically during `pnpm build` (mode=production).
# Set to the absolute API origin so the built bundle can find the API when
# served by `vite preview` or any other static host.
VITE_API_BASE_URL=http://localhost:9090
